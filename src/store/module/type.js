const state = {
    carId:''
}

const getters = {

}

const mutations = {
   changeCarId(state, payload){
       
   }
}
const actions = {
    
}
export default {
    namespaced:true,
    state,
    getters,
    actions,
    mutations
}